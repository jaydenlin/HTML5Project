/*global define*/

define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    'use strict';

    var WiwiModel = Backbone.Model.extend({
        defaults: {
        }
    });

    return WiwiModel;
});
